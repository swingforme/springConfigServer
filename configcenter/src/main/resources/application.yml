server:
  port: 10000
spring:
  application:
    name: spring-config-server
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
  profiles:
    active: native
encrypt:
  key: hm
